// Canonical production origin. Metadata, robots, and the sitemap all read from
// here so they can never drift apart. Canonical URLs always point at production
// even from preview deploys — that's the point of a canonical.
export const SITE_URL = "https://tarazu.app";

export const SITE_NAME = "Tarazu";
