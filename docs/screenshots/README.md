Drop UI captures here. The README references multiple captures from this directory:

- `hero.png` — the Priorities list with RICE-scored candidates and the AI Decision Advisor
- `map.png` — the Effort vs. Impact priority map with labeled quadrants
- `candidate-detail.png` — the per-candidate RICE breakdown and metadata
- `score-to-recommend.gif` — scoring a backlog, then generating an AI recommendation
